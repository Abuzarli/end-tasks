let number = 4;
pipe(number);

{
pipes = "";
space = "";
for (let i = 0; i < num; i++)
{
    pipes = pipes + "|";
    for (let j = i; j < num - 1; j++)
    {
        space = space + " ";
    }
    console.log();(space + pipes);
    space = "";
}
}
